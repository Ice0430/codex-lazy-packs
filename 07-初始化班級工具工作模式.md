# Codex 懶人包 #07：開始你的專案

> 版本：v0.4（Codex 版）
> 更新日期：2026-05-01
> 對應影片：Claude基本功 EP10「新手從零開始你的專案／老師建專案指南」（Codex 平移版）

公開網址：
- Repo root：https://github.com/mathruffian-dot/codex-lazy-packs
- 本檔：https://github.com/mathruffian-dot/codex-lazy-packs/blob/master/07-%E5%88%9D%E5%A7%8B%E5%8C%96%E7%8F%AD%E7%B4%9A%E5%B7%A5%E5%85%B7%E5%B7%A5%E4%BD%9C%E6%A8%A1%E5%BC%8F.md

---

## 這個懶人包會幫你做什麼？

這是一份「工作模式啟動包」。跑完之後，你就可以用 Codex 管理一個教學應用專案：

- 建好 Google Drive / OneDrive 裡的工作資料夾
- 建好 `AGENTS.md`，讓 Codex 每次都知道專案規則
- 建好 Obsidian 專案駕駛艙，記錄進度、下一步、踩坑
- 建好 GitHub repo，必要時啟用 GitHub Pages
- 套用 Codex 的三個全域流程：
  - `project-init-sync`：新專案初始化
  - `startup-sync`：開工接續
  - `shutdown-sync`：收工同步
- 讓專案準備好，接著回到影片教學，跟著做第一個工具

> 一句話：Claude 版靠 `/開工`、`/收工` skills；Codex 版改用內建的 `開工`、`收工`、`新專案初始化` 三個全域 skill 流程。

---

## Claude 版改成 Codex 版時，要改哪些地方？

| Claude Code 版 | Codex 版 |
|---|---|
| `CLAUDE.md` | `AGENTS.md` |
| `~/.claude-skills/startup/` | `~/.codex/skills/startup-sync/` |
| `~/.claude-skills/shutdown/` | `~/.codex/skills/shutdown-sync/` |
| 手動新增 `/開工`、`/收工` skill | 使用全域觸發詞：「開工」、「收工」 |
| SessionEnd hook | Codex 目前不使用 SessionEnd hook，改成明確說「收工」 |
| bash 範例 | PowerShell 範例 |
| Claude Code 設定 | Codex `~/.codex/config.toml`、`~/.codex/skills/` |

保留不變的核心概念：

- 工作桌 + 三個家
- AGENTS / Obsidian 分工
- GitHub repo 管版本與上線
- Firebase 管互動資料
- 學生資料只用座號與班級代號，不放真名

---

## 先備條件

使用前請先確認：

- [ ] Codex Desktop / IDE / CLI 已安裝、已登入
- [ ] Git 已安裝
- [ ] GitHub CLI 已安裝並登入
- [ ] Obsidian vault 已建立
- [ ] Codex 已連到 Obsidian MCP
- [ ] Firebase MCP 已連好（若專案會用資料庫）
- [ ] Codex 全域 skills 內有：
  - `startup-sync`
  - `shutdown-sync`
  - `project-init-sync`

PowerShell 快速檢查：

```powershell
git --version
gh auth status
node --version

Test-Path "$env:USERPROFILE\.codex\skills\startup-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\shutdown-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\project-init-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\config.toml"
```

如果 Firebase MCP 還沒裝，先跑懶人包：

- `04.5-連接-Firebase-資料庫.md`

如果 Obsidian MCP 還沒裝，先跑懶人包：

- `02.5-連接GitHub與Obsidian.md`

---

## 使用方式

把這份 MD 檔交給 Codex，然後說：

```text
請照這份 #07 懶人包，幫我開始一個新的教學工具專案。先檢查環境，再問我專案名稱、用途、工作資料夾、GitHub repo、Obsidian 駕駛艙位置，然後用「新專案初始化」流程建立。
```

如果你已經知道專案資料，可以直接這樣說：

```text
請照 #07 懶人包幫我初始化新專案。
專案名稱：my-classroom-tools
用途：放我的班級互動工具
工作資料夾：G:\我的雲端硬碟\my-classroom-tools
GitHub repo：mathruffian-dot/my-classroom-tools
repo 類型：公開，因為我要用 GitHub Pages
Obsidian vault：C:\Users\<你>\OneDrive\文件\Secondbrain
Obsidian 駕駛艙：my-classroom-tools/專案工作流程.md（vault 內相對路徑）
部署目標：GitHub Pages
```

> 注意：工作資料夾不要放在 Obsidian vault 裡。Obsidian 是「想法的家」，只放 `專案工作流程.md` 這類駕駛艙筆記；真正的專案檔案請放在 Google Drive / OneDrive 的工作資料夾。

> [!tip]
> 如果是把 #07 套用到「已存在」的專案，不要重跑整套初始化。先檢查現況，再補缺口：`AGENTS.md`、`README.md`、`.gitignore`、Obsidian 駕駛艙、GitHub remote、開工 / 收工 skills。已存在的網頁、Firebase 設定、Git 歷史都要保留。

---

## 階段一：環境檢查

請 Codex 先執行下列檢查，不要一開始就改檔：

```powershell
git --version
gh auth status
node --version

Test-Path "$env:USERPROFILE\.codex\skills\startup-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\shutdown-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\project-init-sync\SKILL.md"
```

接著確認 Obsidian vault 路徑。常見位置：

```powershell
Test-Path "C:\Users\<你的帳號>\OneDrive\文件\Secondbrain"
Test-Path "C:\Users\<你的帳號>\Documents\<你的vault名>"
```

Codex 應回報：

- Git 是否可用
- GitHub 是否已登入
- Node 是否可用
- 三個 Codex skills 是否存在
- Obsidian vault 是否找得到
- Firebase MCP 是否已設定（如果這個專案要用 Firebase）

---

## 階段二：問清楚專案資料

Codex 應先問這些問題，再開始建立檔案：

1. 專案名稱是什麼？
2. 專案用途是什麼？
3. 工作資料夾要放哪裡？
4. 要不要建立 GitHub repo？
5. repo 要公開還是私有？
6. 是否要啟用 GitHub Pages？
7. Obsidian 駕駛艙要放哪裡？
8. 是否會使用 Firebase？
9. 是否有部署目標，例如 GitHub Pages、Firebase、Vercel？

教學工具若要用 GitHub Pages，repo 通常要公開。若只是私人備份，repo 預設私有。

---

## 階段三：使用 Codex 的「新專案初始化」流程

當資料確認完，請 Codex 執行 `project-init-sync` 流程。

使用者可以直接說：

```text
新專案初始化
```

Codex 要建立或補齊：

- 專案根目錄 `AGENTS.md`
- Obsidian 專案駕駛艙
- `README.md`
- `.gitignore`
- Git repo
- GitHub repo（若使用者需要）
- 開工 / 收工規則

如果專案已經存在，Codex 要先報告「已存在 / 缺少」清單，再只補缺少的項目。不要覆蓋既有 `AGENTS.md`、`.gitignore`、README 或 Firebase 設定；若要改，先保留原有專案規則，再小幅補強。

---

## AGENTS.md 建議內容

在專案根目錄建立 `AGENTS.md`：

```markdown
# <專案名稱> — AGENTS.md

## 專案入口

專案名稱：<專案名稱>
專案用途：<用途>
主要工作目錄：<絕對路徑>
GitHub repo：<repo URL 或待建立>
預設 branch：main

## Obsidian 對應筆記

Obsidian vault：<vault 絕對路徑，例如 C:\Users\<你>\OneDrive\文件\Secondbrain>
專案駕駛艙：<vault 內相對路徑，例如 my-classroom-tools/專案工作流程.md>
收工時優先更新：同上

> 注意：專案駕駛艙是 Obsidian vault 裡的一篇筆記，不是工作資料夾裡的 Markdown 檔。

## 工作桌 + 三個家

- 工作桌：<Google Drive / OneDrive 工作資料夾>
- GitHub：<repo URL>
- Obsidian：<vault 絕對路徑> + <駕駛艙 vault-relative path>
- Firebase：<Firebase project id；若沒有就寫未使用>

### 耐久回寫原則
- 專案中的 durable findings、踩坑、關鍵決策與已驗證結果，不只留在聊天裡。
- 當使用者說「儲存到它該儲存的地方」時，預設依專案情境回寫到 Firebase、Obsidian、GitHub。
- Obsidian 優先承接人可讀的進度、決策、踩坑與知識整理。
- GitHub 優先承接 repo 內規則、README、docs、issue fix、程式與可版本化文件。
- Firebase 優先承接屬於產品或系統狀態的資料、設定或經驗證結果；若專案沒有 Firebase，則明確略過，不硬寫。

## 同步規則

開工時：
- 使用 `startup-sync` 流程
- 讀本檔
- 讀 Obsidian 駕駛艙
- 讀取本專案的耐久回寫目標（Firebase / Obsidian / GitHub）
- 檢查 Git 狀態
- 不自動 pull / commit / push

收工時：
- 使用 `shutdown-sync` 流程
- 盤點本次有哪些 durable findings、踩坑、關鍵決策或已驗證結果需要回寫
- 更新 Obsidian 駕駛艙
- 依專案情境把該寫回的內容同步到 Firebase、Obsidian、GitHub
- 如規則、路徑、專案邊界改變才更新本檔
- 需要時 commit + push GitHub

新專案初始化時：
- 使用 `project-init-sync` 流程

## 主要檔案

入口檔：
設定檔：
部署位置：

## 不要做

- 不要把每日進度寫進 AGENTS.md
- 不要自動納入無關 git 變更
- 不要把 API key、token、密碼寫進 repo
- 不要儲存學生姓名；正式資料只用座號與班級代號
```

---

## .gitignore 建議內容

```gitignore
# 系統檔
desktop.ini
Thumbs.db
.DS_Store
*.tmp
~$*

# 敏感資料
.env
.env.*
*.key
credentials.*
secrets.*

# AI agent 本地設定
.claude/
.codex/

# Node.js
node_modules/
package-lock.json

# 建置輸出
dist/
build/
.cache/
```

---

## Obsidian 駕駛艙建議內容

請用 Obsidian MCP 在 Obsidian vault 裡建立筆記，不要在專案工作資料夾裡另外建立一份進度檔。

需要記兩個路徑：

| 類型 | 範例 | 用途 |
|------|------|------|
| Obsidian vault 絕對路徑 | `C:\Users\<你>\OneDrive\文件\Secondbrain` | Codex / MCP 找到筆記本根目錄 |
| 駕駛艙 vault 內相對路徑 | `<專案名稱>/專案工作流程.md` | AGENTS.md、開工、收工要讀寫的筆記 |

在 Obsidian vault 建立：

`<專案名稱>/專案工作流程.md`

```markdown
# <專案名稱> 專案工作流程

> 進度、下一步、踩坑與收工紀錄放這裡。
> 專案規則與固定入口看工作資料夾的 `AGENTS.md`。

## 上次做到哪

**最後動作：** 初始化專案
**狀態：** 待開始

## 下一步

1. 回到影片教學段落，跟著做第一個工具。
2. 完成第一個工具後，再請 Codex 預覽、修改與整理 README。
3. 需要上線時，再請 Codex push 到 GitHub 並啟用 GitHub Pages。

## 工具清單

| 工具 | 路徑 | 狀態 |
|------|------|------|
| （尚未建立） | — | #07 只設定工作模式；工具留給影片教學 |

## 最近更動紀錄

| 日期 | 變更摘要 |
|------|----------|
| YYYY-MM-DD | 建立專案駕駛艙 |

## 踩坑筆記

- （之後遇到坑再補）
```

---

## GitHub repo 建立流程

若要用 GitHub Pages，建議公開 repo。

PowerShell 範例：

```powershell
Set-Location "G:\我的雲端硬碟\<專案名稱>"
git init
git config windows.appendAtomically false
git add AGENTS.md README.md .gitignore
git commit -m "初始化教學工具專案"
git branch -M main
gh repo create <repo名> --public --source=. --push
```

如果不需要 Pages，可以改成私有：

```powershell
gh repo create <repo名> --private --source=. --push
```

> Google Drive 內的 git repo 建議先設 `git config windows.appendAtomically false`，可避免 `cannot update the ref` 之類的同步衝突。

---

## 階段四：驗證開工 / 收工

初始化完成後，重開一個 Codex 對話或在同一個對話中測：

```text
開工
```

預期 Codex 會使用 `startup-sync`：

- 同步或檢查 chezmoi
- 讀專案 `AGENTS.md`
- 讀 Obsidian 駕駛艙
- 檢查 Git 狀態
- 建議下一步

再測：

```text
收工
```

預期 Codex 會使用 `shutdown-sync`：

- 盤點本次工作
- 更新 Obsidian 駕駛艙
- 判斷哪些 durable findings、踩坑、關鍵決策或已驗證結果需要回寫到 Firebase、Obsidian、GitHub
- 判斷是否需要更新 `AGENTS.md`
- 檢查 Git diff
- 只提交本次相關檔案
- commit + push
- 回報 Firebase、Obsidian、AGENTS.md、GitHub 同步結果

---

## 階段五：回到影片，跟著做第一個工具

#07 到這裡就完成了。它只負責把專案工作模式設定好，不負責真的做出第一個工具。

接下來請回到影片教學段落，讓使用者跟著影片一步一步做第一個工具。這樣比較適合教學節奏：

- #07 懶人包：建立工作桌、AGENTS.md、Obsidian 駕駛艙、Git / GitHub 入口、開工 / 收工流程。
- 影片實作：示範如何對 Codex 下需求、預覽、修改、上線。

完成 #07 後，Codex 應回報：

```text
專案工作模式已設定完成。
接下來請回到影片，跟著做第一個工具。
之後每次開始可說「開工」，結束可說「收工」。
```

---

## 完成後的工作節奏

| 你說的話 | Codex 會做的事 |
|---|---|
| 「開工」 | 讀 AGENTS.md、讀 Obsidian、檢查 Git、建議下一步 |
| 「我想做一個 XXX 工具」 | 在影片實作或後續工作中，依現有專案規則實作 |
| 「請預覽」 | 啟動本地預覽或用瀏覽器檢查 |
| 「請 push」 | 檢查 diff、commit、push |
| 「查 Firestore 資料」 | 用 Firebase MCP 查資料 |
| 「收工」 | 更新 Obsidian、判斷是否回寫 Firebase / GitHub、必要時更新 AGENTS.md、commit + push |

---

## 2026-05-01 實測修正：套用到既有專案

這次把 #07 套用到既有 repo `mathruffian-dot/2026database`，實測流程如下：

- 下載到本機的 Markdown 可能因編碼顯示成亂碼；不要照亂碼操作，改讀 GitHub raw 原始檔。
- 既有專案不要重跑完整「新專案初始化」；先盤點已存在項目，再補缺口。
- 本例已存在：Git repo、GitHub remote、`AGENTS.md`、Obsidian 駕駛艙、Firebase 設定。
- 本例缺少：`README.md`，且 `.gitignore` 還沒有完整納入 `.codex/`、`.env.*`、`secrets.*`、建置輸出。
- 實作時只補 `README.md`、整理 `.gitignore`、在 `AGENTS.md` 的資料夾結構與重要檔案表加入 `README.md`。
- Obsidian 駕駛艙只寫進度紀錄，`AGENTS.md` 只寫固定規則，避免雙寫漂移。
- GitHub commit 只納入本次相關檔案：`AGENTS.md`、`.gitignore`、`README.md`。

---

## 常見問題

| 問題 | 解法 |
|---|---|
| `gh repo create` 失敗 | 先跑 `gh auth status`，未登入就 `gh auth login` |
| GDrive 內 commit 報錯 | 跑 `git config windows.appendAtomically false` 後重試 |
| 新 repo 跑 `git config windows.appendAtomically false` 出現 `fatal: not in a git directory` | 先 `git init`，再跑 `git config windows.appendAtomically false` |
| 文件寫 main 但 Git 顯示 master | 第一次 commit 後執行 `git branch -M main` |
| 「開工」沒觸發 | 確認 `C:\Users\<你>\.codex\skills\startup-sync\SKILL.md` 存在 |
| 「收工」沒觸發 | 確認 `C:\Users\<你>\.codex\skills\shutdown-sync\SKILL.md` 存在 |
| 「新專案初始化」沒觸發 | 確認 `C:\Users\<你>\.codex\skills\project-init-sync\SKILL.md` 存在 |
| Obsidian 讀不到 | 確認 `~/.codex/config.toml` 有 `[mcp_servers.obsidian]`，改完要重開 Codex |
| 專案駕駛艙被建在工作資料夾 | 這是把 Obsidian 駕駛艙誤當成 repo 內文件；應改成建在 Obsidian vault，例如 `Secondbrain/<專案名稱>/專案工作流程.md`，repo 內 AGENTS.md 只記 vault 與相對路徑 |
| 只寫 `my-project/專案工作流程.md`，不知道是哪個 vault | AGENTS.md 要同時記 `Obsidian vault` 絕對路徑與 `專案駕駛艙` vault-relative path |
| Firebase 工具看不到 | 先跑 #04.5；Windows 建議 MCP command 用 `npx.cmd`，改完要重開 Codex |
| 不想公開 repo | 不使用 GitHub Pages 時可建立 private repo |
| 要跨電腦 | 工作檔靠 GDrive / OneDrive，同步設定靠 chezmoi；新電腦先跑 Codex 環境懶人包 |
| 下載的 `.md` 打開是亂碼 | 下載檔或讀取工具的編碼不一致 | 回 GitHub raw 原始檔讀取，不要直接依亂碼版本修改 |
| 已有專案要套 #07 | 不是空資料夾，已有規則與歷史 | 先檢查現況，只補缺少的 `README.md`、`.gitignore`、Obsidian 紀錄等，不要覆蓋既有設定 |
| `AGENTS.md` 顯示未追蹤 | 舊專案可能原本只有 `CLAUDE.md`，或 AGENTS 尚未納入 Git | 若 #07 要讓 Codex 使用 AGENTS，確認內容無敏感資訊後納入本次 commit |
| `.gitignore` 註解或中文顯示亂碼 | 舊檔案編碼或終端顯示問題 | 可保留規則本體，將短檔案整理成 UTF-8、繁中註解清楚的安全版本 |
| README 缺少 | 舊示範專案可能直接靠 HTML 與 AGENTS 運作 | 補一份簡短 README：用途、主要檔案、工作模式、安全原則即可 |
| GitHub API 更新中文檔名失敗 | PowerShell / Node 管線可能把中文檔名轉成問號 | 對 GitHub contents API 使用 URL encoded path，或用純 PowerShell Unicode 流程 |
| GitHub 文件被壓成一長行 | 讀 raw 時沒有保留換行 | 用 `$Lines = gh api ...` 後 `$Content = $Lines -join "`n"` 再更新 |

---

## 安全與隱私

- `.codex/`、`.claude/` 不要 commit
- `.env`、API key、token、密碼不要 commit
- Firebase 前端 config 可以公開，但 Admin 憑證不可以
- 學生資料只存座號與班級代號，不存真名
- AGENTS.md 記規則；Obsidian 記進度；不要雙寫漂移

---

## 更新紀錄

| 日期 | 版本 | 更新內容 |
|------|------|---------|
| 2026-05-01 | v0.4 | 釐清專案駕駛艙必須建立在 Obsidian vault 內，AGENTS.md 要同時記錄 vault 絕對路徑與 vault 內相對筆記路徑，避免誤建到工作資料夾 |
| 2026-05-01 | v0.3 | 補充套用到既有專案的實測修正：下載 Markdown 亂碼時改讀 GitHub raw、先盤點再補缺口、README / .gitignore / AGENTS.md 的最小修正、Obsidian 只記進度、GitHub API 中文檔名與換行處理坑 |
| 2026-05-01 | v0.2 | 改成 Codex 全域 skills 版：`project-init-sync`、`startup-sync`、`shutdown-sync`；移除 Claude 專屬 skill / hook / bash 腳本，改用 PowerShell 與 AGENTS.md；明確定位 #07 只設定工作模式，不建立第一個工具 |
| 2026-04-26 | v0.1 | 初版 Codex 平移版 |

---

## 相關懶人包

- `02-連接-GitHub.md`
- `02.5-連接GitHub與Obsidian.md`
- `03-建立第二大腦-Obsidian.md`
- `04.5-連接-Firebase-資料庫.md`
- `09-用chezmoi同步Codex設定.md`
---

## 2026-05-04 實測補充：2026DataBase 初始化踩坑與修正

### 本次實測目標

- 專案資料夾：`G:\我的雲端硬碟\2026DataBase`
- GitHub repo：`https://github.com/Ice0430/2026DataBase`
- Firebase project：`teacherstudy-25551`
- Firebase Hosting：`https://teacherstudy-25551.web.app`
- Obsidian vault：`G:\我的雲端硬碟\Secondbrain`
- Obsidian 專案筆記：`2-專案\2026DataBase\班級工具工作模式.md`

### 坑 1：指南要求三個 skills，但 repo 沒有可直接複製的範本

實測時三個檢查都回傳 `False`：

```powershell
Test-Path "$env:USERPROFILE\.codex\skills\startup-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\shutdown-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\project-init-sync\SKILL.md"
```

修正：懶人包 repo 已新增三個範本：

```text
skills/startup-sync/SKILL.md
skills/shutdown-sync/SKILL.md
skills/project-init-sync/SKILL.md
```

安裝方式：

```powershell
$Source = "G:\我的雲端硬碟\codex-lazy-packs\skills"
$Target = "$env:USERPROFILE\.codex\skills"
Copy-Item -Recurse -Force "$Source\startup-sync" $Target
Copy-Item -Recurse -Force "$Source\shutdown-sync" $Target
Copy-Item -Recurse -Force "$Source\project-init-sync" $Target
```

驗證：

```powershell
Test-Path "$env:USERPROFILE\.codex\skills\startup-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\shutdown-sync\SKILL.md"
Test-Path "$env:USERPROFILE\.codex\skills\project-init-sync\SKILL.md"
```

三行都應該回傳 `True`。

### 坑 2：不要把使用者層級的 skills commit 到專案 repo

`startup-sync`、`shutdown-sync`、`project-init-sync` 是使用者層級設定，實際安裝位置在：

```text
C:\Users\whatl\.codex\skills\...
```

它們不應該提交到班級工具專案 repo。班級工具專案 repo 只需要提交自己的專案檔，例如：

```text
AGENTS.md
README.md
.gitignore
firebase.json
firestore.rules
public/
```

### 坑 3：Obsidian 專案筆記應該放在 vault，而不是 app repo

本次正確位置：

```text
G:\我的雲端硬碟\Secondbrain\2-專案\2026DataBase\班級工具工作模式.md
```

`AGENTS.md` 裡只記錄 vault 路徑與 vault-relative path，不要把整個 Obsidian vault 複製進 app repo。

### 坑 4：專案已存在時，不要重跑初始化覆蓋既有內容

本次 `2026DataBase` 已經有：

```text
public/
firebase.json
firestore.rules
.firebaserc
.gitignore
README.md
.git/
```

所以 #07 初始化應採取「補齊缺少項目」：

- 新增 `AGENTS.md`
- 建立 Obsidian 專案筆記
- 建立缺少的 Codex skills
- 驗證 Firebase / GitHub / Hosting
- 只提交本 repo 內新增的 `AGENTS.md`

不要重建 Firebase 專案、不要覆蓋網頁、不要重設 GitHub remote。

### 坑 5：Google Drive repo 提交前要確認工作區範圍

本次只提交 app repo 內的：

```text
AGENTS.md
```

提交前固定檢查：

```powershell
git status --short
git diff --cached --stat
```

若有 Obsidian vault 或 `$env:USERPROFILE\.codex\skills` 的變更，這些不屬於 app repo，不要硬塞進 app repo commit。

### 坑 6：驗證應包含外部服務狀態

#07 完成前至少做這些驗證：

```powershell
git --version
gh auth status
node --version
npx.cmd -y firebase-tools@latest use
curl.exe -I https://teacherstudy-25551.web.app
```

本次驗證結果：

- Git 可用。
- GitHub CLI 已登入 `Ice0430`。
- Node 可用。
- Firebase project 為 `teacherstudy-25551`。
- Firebase Hosting 回應 `200 OK`。
- 三個 Codex skills 均存在。
- Obsidian 專案筆記存在。

### #07 實測完成提交

班級工具專案 repo：

```text
https://github.com/Ice0430/2026DataBase
```

提交：

```text
6f3dde4 Add classroom workflow instructions
```

結論：#07 的實作方式應是「檢查現況後補齊缺口」，不是假設空資料夾重新初始化。懶人包已補上三個可安裝 skill 範本，之後照指南執行時可以直接複製安裝。

---

## 2026-05-05 實測補充：Firebase 資料庫部署與讀寫驗證

### 本次實測目標

使用 #04.5 的 Firebase 設定資料，回到 `2026DataBase` 專案確認資料庫設定可用，並驗證 Codex 能新增、讀取、更新、刪除 Firestore 資料。

本次確認的專案設定：

- 專案資料夾：`G:\我的雲端硬碟\2026DataBase`
- Firebase project：`teacherstudy-25551`
- Firestore collection：`wordcloud_words`
- Hosting URL：`https://teacherstudy-25551.web.app`

### 坑 7：設定檔正確，不代表 Firebase CLI 已登入

本次三個核心檔案都已正確：

```text
.firebaserc      -> teacherstudy-25551
firebase.json    -> firestore.rules + hosting public
firestore.rules  -> 只開放 wordcloud_words
```

但第一次執行部署仍失敗：

```text
Error: Failed to authenticate, have you run firebase login?
```

修正方式：

1. 請使用者在自己的 PowerShell / CMD 互動登入。
2. Codex 不要硬跑 `firebase login`，因為 Codex 非互動環境常會失敗。
3. 登入完成後再由 Codex 執行驗證與部署。

Windows 指令：

```powershell
cd "G:\我的雲端硬碟\2026DataBase"
npx.cmd -y firebase-tools@latest login
```

### 坑 8：Windows PowerShell 一律優先用 `npx.cmd`

不要用：

```powershell
npx -y firebase-tools@latest deploy --only firestore:rules
```

在 Windows PowerShell 容易踩到 `npx.ps1` 執行原則或路徑問題。

建議固定用：

```powershell
npx.cmd -y firebase-tools@latest use
npx.cmd -y firebase-tools@latest deploy --only firestore:rules
npx.cmd -y firebase-tools@latest deploy --only hosting
```

### 坑 9：部署前先確認 CLI 指到正確 project

本次先跑：

```powershell
npx.cmd -y firebase-tools@latest use
```

正確輸出應為：

```text
teacherstudy-25551
```

再部署 Firestore rules：

```powershell
npx.cmd -y firebase-tools@latest deploy --only firestore:rules
```

本次成功訊息：

```text
cloud.firestore: rules file firestore.rules compiled successfully
firestore: released rules firestore.rules to cloud.firestore
Deploy complete!
```

### 坑 10：只部署 rules 不算完成，要做資料庫 CRUD 驗證

本次用 Firestore REST API 驗證 `wordcloud_words` collection 可以被 Codex 操控。

測試流程：

1. 新增測試文件。
2. 讀取測試文件。
3. 更新 `count` 與 `status`。
4. 列出 `wordcloud_words` 前幾筆資料。
5. 刪除測試文件。
6. 再讀一次確認回應 404，避免留下測試垃圾資料。

本次結果：

```json
{
  "project": "teacherstudy-25551",
  "collection": "wordcloud_words",
  "createdDocument": "codex_control_test_20260505_130906",
  "readText": "codex_control_test_20260505_130906",
  "updatedCount": "2",
  "updatedStatus": "updated",
  "listedDocuments": 4,
  "deleteVerified": true
}
```

判定：

- Codex 可以新增 Firestore 文件。
- Codex 可以讀取 Firestore 文件與 collection。
- Codex 可以更新 Firestore 文件。
- Codex 可以刪除測試文件。
- 測試結束後要確認 `deleteVerified = true`。

### 坑 11：既有專案套用 #07 時，不要重建 Firebase

若專案已存在 `.firebaserc`、`firebase.json`、`firestore.rules`、`public/app.js`，先讀檔比對，不要直接覆蓋。

本次正確處理方式：

- 保留 `.firebaserc` 指向 `teacherstudy-25551`。
- 保留 `firebase.json` 既有 hosting 設定，只確認有 Firestore rules。
- 保留 `firestore.rules` 只開放 `wordcloud_words`。
- 保留前端 `public/app.js` 的 Firebase config。
- 只在使用者登入 Firebase CLI 後部署 rules。

### 可直接複用的驗證清單

```powershell
git status -sb
Get-Content .firebaserc
Get-Content firebase.json
Get-Content firestore.rules
npx.cmd -y firebase-tools@latest use
npx.cmd -y firebase-tools@latest deploy --only firestore:rules
```

完成後再做一次 CRUD 測試，並刪除測試資料。
---

## 2026-05-07 實測補充：初始化後的安全補強與 Git lock 處理

這次重檢 `2026DataBase` 時，#07 的主要設定都已存在，但仍補到兩個容易漏掉的收尾項目。

### 坑 1：`.gitignore` 不能只排 Firebase cache

原本 `.gitignore` 只有 `.firebase/`、debug log、`node_modules/` 等項目，缺少本機敏感檔與 agent 設定。初始化或複檢時應確認至少包含：

```gitignore
.env
.env.*
*.key
credentials.*
secrets.*
.codex/
.claude/
desktop.ini
Thumbs.db
.DS_Store
*.tmp
~$*
```

本次已在 `2026DataBase` 補上並提交：`f26eaa3 Tighten local ignore rules`。

### 坑 2：Google Drive 內的 Git repo 可能殘留 lock file

提交 `.gitignore` 時，commit 實際已建立，但 Git 收尾時出現：

```text
Unable to create '.git/packed-refs.lock': File exists.
```

處理方式：

1. 先用 `git log -1 --oneline` 確認 commit 是否已成功建立。
2. 用 `git status -sb` 確認工作樹狀態。
3. 檢查 `.git/packed-refs.lock` 的時間與大小；若是早於本次操作的 0-byte 殘留鎖檔，才刪除。
4. 刪除前確認路徑在本 repo 的 `.git` 目錄內，不要用模糊路徑或遞迴刪除。
5. 刪除後再 `git push origin main`。

另外，Google Drive 專案建議設定：

```powershell
git config windows.appendAtomically false
```

本次 `2026DataBase` 已設定並推送成功，最後狀態為 `main...origin/main`。


## 2026-05-17 Obsidian MCP 與週整理踩坑補充

### 坑 1：MCP 可能存在但尚未延遲載入

- 初始資源清單沒有看到 Obsidian，不代表 MCP 壞掉。
- 先喚醒對應能力，再以 `get_vault_stats -> write_note -> read_note` 做端到端驗證。
- 判準要看實際讀寫是否成功，不要只看工具清單有沒有先顯示。

### 坑 2：刪除筆記前先核對實際檔名

- 使用者口述的路徑可能和實際檔名有大小寫或空白差異。
- 先搜尋真實存在的筆記，再刪除、再驗證不存在。

### 坑 3：週整理流程若遇到保護限制，限制優先

- 如果使用者要求「既有頁面若要更新，先列計畫，不要直接改」，那就不能因為流程步驟寫了 `index.md` / `log.md` 而硬改。
- 正確做法是：新增新知識頁與補充週報；對既有頁面先列更新計畫。

### 坑 4：調整排程時更新既有 automation，不要重建

- 先找現有 `weekly-knowledge-review`，再把星期天 09:17 改成星期六 22:00。
- 修改後要回查，確認生效的是同一個 automation 的新排程。

### 與耐久回寫原則的關係

- 這類跨專案會重複出現的工作流踩坑，應寫回 Obsidian 的知識頁與 GitHub 來源文件。
- 若某次經驗屬於產品資料或系統狀態，才同步評估是否需要寫回 Firebase；沒有對應 Firebase 目標時，不硬寫。
